
{
  // This task has been automatically generated by
  // {$userHome}/.vscode/scripts/setup-r-projects
  //
  // When the VSCode folder is open, this task opens a split terminal 
  //   with git bash on the left and R terminal on the right
  //
  // This task is automatically added to the project only if an R folder is there
  //  or the workspace root contains an .R file, or at least one direct childern folder
  //  contains an .R file
  //
  // See ctrl+shift+P -> Tasks: Open user tasks -> "Setup R projects watcher"
  //
  
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Create terminals",
      // => will call these two tasks
      "dependsOn": [
        "git bash",
        "R-term"
      ],
      // => starts the task when opening VSCode
      "runOptions": {
        "runOn": "folderOpen"
      }
    },
    {
      "label": "git bash",
      "type": "shell",
      "command": "",
      "options": {
        "shell": {
          // NOTA: path/to/git/bin must be on PATH
          "executable": "sh.exe",
          "args": ["--login"]
        }
      },
      // Mark as a background task to avoid the spinner animation on the terminal tab
      "isBackground": true,
      "problemMatcher": [],
      // Create the tasks in a terminal group
      "presentation": {
        "echo": false,
        "group": "my-group"
      }
    },
    {
      "label": "R-term",
      "type": "shell",
      "command": "",
      "options": {
        "shell": {
          // NOTA: to attach R properly to VSCode, you need the custom .Rprofile
          // that is why you should have an Rprofile set up properly
          // see task "Write VSCode Rprofile"
          "executable": "radian.exe",
          "args": ["--no-save", "no-restore"]
        }
      },
      "isBackground": true,
      "problemMatcher": [],
      "presentation": {
        "echo": false,
        "group": "my-group"
      }
    }
  ]
}


